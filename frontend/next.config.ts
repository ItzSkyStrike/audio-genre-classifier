import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ['172.18.218.203'],

  // (Keep any other existing config options inside this object)
};

export default nextConfig;